#include "main.h"
/*
 *main - Entry point.
 *
 * Return: Always  (Success)
 */
int main (void)
{
	char *str = "_putchar\n";

	for (int i = 0; str(i) ! '\0'; i++)
	{
		_putchar(str[i]);
	}
	return (0);
}
