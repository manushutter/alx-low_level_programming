#include "main.h"
/**
 * int _islower(int c) - checks for lowercase character
 *
 * Return 1 when c is lowercase and 0 otherwise
 */
int _islower(int c)
{
	char letter = 'a';

	while (letter <= 'z')
	{
		_putchar(letter);
		letter++;
		if(c = 'c')
		{
			return (1);
		}
		else
		{
			return (0);
		}
	}
}
