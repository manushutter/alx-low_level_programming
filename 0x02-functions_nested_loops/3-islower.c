#include "main.h"
/**
 * int _islower(int c) - checks for lowercase character
 *
 * Return 1 when c is lowercase and 0 otherwise
 */
int _islower(int c)
{
	char letter = 'a';
	int c;

	while (letter < 'z')
	{
		_putchar(letter);
		letter++;
		if (letter = 'C')
		{
			return (1);
		}
		else
		{
			return (0);
		}
	}
}
